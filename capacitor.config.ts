import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'wealthsimple',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
